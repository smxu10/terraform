pipeline {
    agent any
    environment {
        AWS_PROFILE = 'default'
    }
    stages {
        stage('terraform') {
            steps {
                dir('ssm/parameter_store'){
                  bat '''
                    terraform init
                  '''
                }
            }
        }
    }
}
